module.exports = {
    singleQuote: true,
    trailingComma: 'all', // , no final de objetos
    allowParens: 'avoid', // não coloca parênteses em arrow functions que só tem 1 parâmetro
    tabWidth: 4
}
